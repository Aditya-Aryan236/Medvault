# Shared defaults for stacks that use env_file: ../.shared.env
# Per-hospital host ports live in each hospital-*/.env (see PORTS.md).
MONGO_VERSION=7.0
MONGO_EXPRESS_VERSION=1.0.2

CENTRAL_API_URL=http://gcp-api:8000